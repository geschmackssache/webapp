import {
  formattedPrice
} from 'geschmackssache/helpers/formatted-price';

module('FormattedPriceHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = formattedPrice(42);
  ok(result);
});
